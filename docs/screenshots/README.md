# Screenshots

Final screenshots are captured against the deployed demo at release time
(Agent 1 / release owner):

- `desktop.png` — full results view at 1280×720 (sample analysis).
- `mobile.png` — results view at 390×844 (Pixel-class viewport).

Until captured, the README image links in the repository root are
placeholders. Use only synthetic sample data in screenshots — never a real
resume.
